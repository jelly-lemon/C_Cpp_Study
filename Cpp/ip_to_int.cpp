/**
 TODO IP转int
* 给出一个合法的IPv4地址，将其解析成一个int32。
*/
#include<string>
#include<iostream>
#include <vector>

using namespace std;

/**
 size_type由string类类型和vector类类型定义的类型，
 用以保存任意string对象或vector对象的长度，
 标准库类型将size_type定义为unsigned类型。
string抽象意义是字符串， size（）的抽象意义是字符串的尺寸，
 string::size_type抽象意义是尺寸单位类型。
 */

int main() {
    int n1, n2, n3, n4;
    string s = "192.168.1.1";

    return 0;
}